# Core runtime dependencies. For optional extras see pyproject.toml:
#   pip install -e ".[llm]"          Claude-backed analysis (anthropic SDK)
#   pip install -e ".[transformer]"  fine-tuning / transformer inference
#   pip install -e ".[api]"          FastAPI service
#   pip install -e ".[dev]"          tests and linting
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
pydantic>=2.0
pyyaml>=6.0
joblib>=1.3
typer>=0.9
